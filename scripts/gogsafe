#!/bin/bash
# gogsafe - safe wrapper for gog CLI with timeout
# Usage: gogsafe <command> [args...]

GOG="${GOG_PATH:-/app/gog}"
TIMEOUT="${GOG_TIMEOUT:-30}"

if [ ! -x "$GOG" ]; then
    echo "Error: gog binary not found or not executable at $GOG"
    exit 1
fi

exec timeout "$TIMEOUT" "$GOG" "$@"
